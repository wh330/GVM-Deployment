version: "3.5"
services:
  scap-sync:
    image: registry.gitlab.developers.cam.ac.uk/uis/infra/gvm-deployment/gvmd
    volumes:
      - gvm-var-lib:/var/lib/gvm
    command: greenbone-scapdata-sync --curl --verbose
    depends_on:
      - "gvmd"
    networks:
      - gvm-network
